import { Vector2 } from "../lib/GameEngine/Vector2";
describe(" ", () => {
        it("testing verctor 0", () => {
                expect(Vector2.zero().getX()).toEqual(new Vector2(0, 0).getX());
        });
});

describe("c'est dlhzofuhzeofga merde jest", () => {
    it("ça devrait puer sa mère juste la", () => {
            expect(1).toEqual(1);
    });
});
